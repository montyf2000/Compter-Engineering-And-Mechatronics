
#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main(void)
{   
    /*
    float r, Volume, Surface;

    printf("What is the Radius of your shever?\n");
    // use scanf with %f to read into 'f' 
    scanf ("%f",&r);  // note the important & 

    
    Volume = (4*M_PI*r*r*r)/3;
    Surface = 4*M_PI*r*r;
    printf("The surface area =%.2f\nThe volume =%.2f\n",Surface,Volume);

    return 0;     // Exit from main
    */


   /*
   float r,h,SA;

   printf("Please enter the radius then height...\n");
   // use scanf with %f to read into 'f'
   scanf ("%f, %f",&r,&h);  // note the important & 

   SA = (2*M_PI*(r*r))+(2*M_PI*r*h);

   printf("The surface area = %.3f\n", SA);

   */
  char name[51];

  scanf("%s",&name);
  printf("Name = %s\n", name[51]);

  

}
